# Ranges (not hard pins) so pip can pick wheels that match the local Python;
# hard-pinning old versions forces numpy<2 source builds on Python 3.13+.
langchain>=0.3.7,<0.4
langchain-openai>=0.2.6,<0.4
langchain-community>=0.3.5,<0.4
langchain-text-splitters>=0.3.2,<0.4
chromadb>=1.0
python-dotenv>=1.0
requests>=2.32
mcp>=1.1
